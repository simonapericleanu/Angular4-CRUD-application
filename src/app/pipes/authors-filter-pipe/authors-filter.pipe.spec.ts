import { AuthorsFilterPipePipe } from './authors-filter.pipe';

describe('AuthorsFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AuthorsFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
